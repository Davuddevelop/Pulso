# Baseline dependencies. Nothing is added here without asking first.
numpy
scipy
soundfile

# Mic capture. Requires the PortAudio system library:
#   Debian/Ubuntu: sudo apt-get install libportaudio2
#   macOS:         brew install portaudio
# Imported lazily by MicSource so that file-only machines (CI, containers)
# can still import sources.py.
sounddevice

# Hardware capture over USB serial (ELEGOO Mega 2560, hardware/pulso_mic.ino).
# New dependency, added when real hardware became available -- flagging it
# rather than adding it silently. Imported lazily by SerialMicSource, same
# reasoning as sounddevice above.
pyserial

matplotlib

# live_server.py: phone-mic-over-WebSocket demo path. New dependency, added
# when the live-mic feature was built -- flagging it rather than adding it
# silently. Only live_server.py imports aiohttp; sources.py/app.py/the core
# pipeline do not depend on it.
aiohttp

# torch is deliberately NOT listed. It is only installed if the CNN happens.
